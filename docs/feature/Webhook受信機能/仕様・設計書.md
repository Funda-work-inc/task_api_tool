# 仕様・設計書：Webhook受信機能

## 📋 基本情報
- **機能名**: Webhook受信機能
- **連携先**: task_learning_app Issue #10
- **作成日**: 2025年12月17日

## 🎨 アーキテクチャ設計

### システム構成
```
┌─────────────────────────────┐
│   task_learning_app         │
│   (Rails 8.0.2.1)           │
│                              │
│  ┌────────────────────┐     │
│  │ WebhookNotifier    │     │
│  │ (Service Object)   │     │
│  └────────────────────┘     │
│           │                  │
└───────────┼──────────────────┘
            │ HTTP POST
            ▼
┌─────────────────────────────┐
│   task_api_tool             │
│   (Sinatra 3.2.0)           │
│                              │
│  POST /webhook              │
│  ┌────────────────────┐     │
│  │ - ペイロード受信   │     │
│  │ - ログ出力         │     │
│  │ - セッション保存   │     │
│  │ - レスポンス返却   │     │
│  └────────────────────┘     │
│                              │
│  GET /webhooks              │
│  ┌────────────────────┐     │
│  │ - 履歴取得         │     │
│  │ - ERBレンダリング  │     │
│  └────────────────────┘     │
└─────────────────────────────┘
```

### データフロー
1. task_learning_app でタスクが作成・更新・削除される
2. WebhookNotifier.notify() が呼ばれる
3. HTTP POSTリクエストで task_api_tool の POST /webhook に送信
4. task_api_tool がWebhookを受信
5. JSONペイロードを解析
6. コンソールにログ出力
7. セッションに履歴を保存（最新10件）
8. 成功レスポンスを返す

## 🔧 詳細設計

### 1. Webhook受信エンドポイント

#### エンドポイント仕様
- **URL**: `POST /webhook`
- **Content-Type**: `application/json`
- **認証**: なし（ローカル環境のため）

#### リクエスト
```json
{
  "event": "created",
  "task": {
    "id": 1,
    "title": "タスク名",
    "priority": "high",
    "due_date": "2025-12-21",
    "completed_at": null,
    "created_at": "2025-12-14T12:00:00Z",
    "updated_at": "2025-12-14T12:00:00Z"
  },
  "timestamp": "2025-12-14T12:00:00Z"
}
```

#### レスポンス

**成功時（200 OK）**:
```json
{
  "success": true,
  "message": "Webhook received"
}
```

**エラー時（400 Bad Request）**:
```json
{
  "success": false,
  "error": "Invalid JSON: 詳細メッセージ"
}
```

**サーバーエラー時（500 Internal Server Error）**:
```json
{
  "success": false,
  "error": "Server error: 詳細メッセージ"
}
```

#### 実装コード（app.rb: 124-161行目）
```ruby
post '/webhook' do
  # リクエストボディを読み取り
  request.body.rewind
  payload = JSON.parse(request.body.read)

  # ログに記録
  puts "=" * 50
  puts "🔔 Webhook受信！"
  puts "イベント: #{payload['event']}"
  puts "タスクID: #{payload['task']['id']}"
  puts "タスク名: #{payload['task']['title']}"
  puts "発生時刻: #{payload['timestamp']}"
  puts "=" * 50

  # セッションにWebhook履歴を保存（最新10件まで）
  session[:webhook_logs] ||= []
  session[:webhook_logs].unshift({
    event: payload['event'],
    task: payload['task'],
    timestamp: payload['timestamp']
  })
  session[:webhook_logs] = session[:webhook_logs].first(10)

  # 成功レスポンスを返す
  status 200
  content_type :json
  { success: true, message: "Webhook received" }.to_json
rescue JSON::ParserError => e
  # JSONパースエラー
  status 400
  content_type :json
  { success: false, error: "Invalid JSON: #{e.message}" }.to_json
rescue => e
  # その他のエラー
  status 500
  content_type :json
  { success: false, error: "Server error: #{e.message}" }.to_json
end
```

### 2. Webhook履歴表示エンドポイント

#### エンドポイント仕様
- **URL**: `GET /webhooks`
- **Content-Type**: `text/html`

#### レスポンス
ERBテンプレート（views/webhooks.erb）を使用してHTML表示

#### 表示内容
- イベント種別（created/updated/destroyed）
- タスクID
- タスク名
- 発生時刻

#### 実装コード（app.rb: 164-167行目）
```ruby
get '/webhooks' do
  @webhook_logs = session[:webhook_logs] || []
  erb :webhooks
end
```

## 🧪 テスト設計（RSpec）

### テストファイル構成
```
spec/
├── spec_helper.rb          # RSpec設定
└── webhook_spec.rb         # Webhookエンドポイントテスト
```

### テストケース一覧

#### POST /webhook エンドポイント

| # | テストケース | 期待結果 |
|---|------------|---------|
| 1 | 正常なWebhookペイロードの場合 | 200 OKを返す |
| 2 | 正常なWebhookペイロードの場合 | 成功レスポンスを返す |
| 3 | 正常なWebhookペイロードの場合 | Content-Typeがapplication/jsonである |
| 4 | 不正なJSONの場合 | 400 Bad Requestを返す |
| 5 | 不正なJSONの場合 | エラーメッセージを含む |
| 6 | createdイベントの場合 | 正常に受信する |
| 7 | updatedイベントの場合 | 正常に受信する |
| 8 | destroyedイベントの場合 | 正常に受信する |

#### GET /webhooks エンドポイント

| # | テストケース | 期待結果 |
|---|------------|---------|
| 1 | Webhook履歴表示ページ | 200 OKを返す |

### テストデータ
```ruby
let(:valid_payload) do
  {
    event: 'created',
    task: {
      id: 1,
      title: 'テストタスク',
      priority: 'high',
      due_date: '2025-12-21',
      completed_at: nil,
      created_at: '2025-12-14T12:00:00Z',
      updated_at: '2025-12-14T12:00:00Z'
    },
    timestamp: '2025-12-14T12:00:00Z'
  }
end
```

### テストツール
- **RSpec**: テストフレームワーク
- **Rack::Test**: HTTPリクエストテスト用
- **rspec-json_expectations**: JSONレスポンス検証用

## 🔍 エラーハンドリング設計

### エラー種別と対応

| エラー種別 | 原因 | HTTPステータス | レスポンス | ログレベル |
|-----------|------|---------------|-----------|------------|
| JSON::ParserError | 不正なJSON | 400 | エラーメッセージを返す | error |
| StandardError | 予期しないエラー | 500 | エラーメッセージを返す | error |

### ログ出力仕様

#### 成功時
```
==================================================
🔔 Webhook受信！
イベント: created
タスクID: 10
タスク名: Webhookテスト
発生時刻: 2025-12-14T11:25:57Z
==================================================
```

## 📁 ファイル構成

```
task_api_tool/
├── app.rb                          # Webhook受信エンドポイント実装
├── views/
│   └── webhooks.erb                # Webhook履歴表示ページ
├── spec/
│   ├── spec_helper.rb              # RSpec設定
│   └── webhook_spec.rb             # Webhookテスト
└── docs/
    └── feature/
        └── Webhook受信機能/
            ├── プロジェクト企画書.md
            ├── 仕様・設計書.md        # 本ドキュメント
            └── 開発記録.md
```

## 🚀 今後の拡張案

### 優先度：高
- [ ] Webhook履歴のDB永続化（SQLite）
- [ ] 履歴の検索・フィルタリング機能

### 優先度：中
- [ ] Webhook受信の統計情報表示
- [ ] ページネーション機能

### 優先度：低
- [ ] Webhook署名による検証
- [ ] APIトークン認証
- [ ] Rate Limiting
